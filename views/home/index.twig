{% extends "layout/default.twig" %}

{% block title %}Index{% endblock %}

{% block content %}
    {% for article in articles %}
    <article>
        <h2>
            <a href="{{ app.url_generator.generate('getArticle', {id: article.id}) }}" >{{article.title|e}}</a>
        </h2>
        <p>{{article.body|e}}</p>
    </article>  
    {% endfor %}
{% endblock %}

{% block javascript %}
    {{parent()}}
    <script type="text/javascript">
    var monjs;
    </script>
{% endblock %}
 